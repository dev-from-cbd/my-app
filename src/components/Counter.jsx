import React, { useState } from "react";

// Define a functional component called "Counter"
const Counter = function () {
  // In this component, we will manage state using the "useState" hook
  // Currently, the component returns an empty <div>, we'll add more logic to it later
  return <div></div>;
};

export default Counter;
